import React from 'react';

const Header = () => {
  return (
    <header>
      <h1>Contact App</h1>
      <p>A simple contact management app built with React</p>
    </header>
  );
};

export default Header;
